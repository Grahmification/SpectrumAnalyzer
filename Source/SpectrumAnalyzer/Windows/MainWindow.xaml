<Window x:Class="SpectrumAnalyzer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SpectrumAnalyzer"
        xmlns:vm="clr-namespace:SpectrumAnalyzer.ViewModels"
        xmlns:UserControls="clr-namespace:SpectrumAnalyzer.UserControls"
        mc:Ignorable="d"
        Title="{Binding Project.WindowTitle}"
        Height="450" Width="800"
        WindowStyle="SingleBorderWindow"
        WindowState="Maximized">

    <Window.DataContext>
        <vm:MainVM/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ==================== Menu bar ==================== -->
        <Menu Grid.Row="0">
            <MenuItem Header="_File">
                <MenuItem Header="_New Project"
                          Command="{Binding Project.NewProjectCommand}"
                          InputGestureText="Ctrl+N"/>
                <MenuItem Header="_Open Project…"
                          Command="{Binding Project.OpenProjectCommand}"
                          InputGestureText="Ctrl+O"/>
                <Separator/>
                <MenuItem Header="_Save"
                          Command="{Binding Project.SaveProjectCommand}"
                          InputGestureText="Ctrl+S"/>
                <MenuItem Header="Save _As…"
                          Command="{Binding Project.SaveProjectAsCommand}"
                          InputGestureText="Ctrl+Shift+S"/>
            </MenuItem>
            <MenuItem Header="_Load Dataset" Command="{Binding LoadDataCommand}">
            </MenuItem>
        </Menu>

        <!-- ==================== Tab content ==================== -->
        <TabControl Grid.Row="1" TabStripPlacement="Top">
            <TabItem Header="Data Configuration">
                <UserControls:DataControl DataContext="{Binding Data}"/>
            </TabItem>
            <TabItem Header="FFT Analysis">
                <UserControls:FFTControl DataContext="{Binding Data.FFT}"/>
            </TabItem>
        </TabControl>

        <!-- ==================== Status bar ==================== -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Background="LightGray">
            <TextBlock Margin="5,2,5,2" VerticalAlignment="Center"
                       Text="{Binding Data.Data.DataFilePath}"/>
        </StackPanel>

    </Grid>
</Window>
